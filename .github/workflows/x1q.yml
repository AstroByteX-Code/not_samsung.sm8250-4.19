name: Build for x1q

on:
  push:
    branches: [ "sm8250" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-x1q:
    uses: ./.github/workflows/build_sm8250.yml
    with:
      device: x1q
      device_name: Galaxy S20 5G
      

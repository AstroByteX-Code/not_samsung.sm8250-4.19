name: Build for z3q

on:
  push:
    branches: [ "sm8250" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-z3q:
    uses: ./.github/workflows/build_sm8250.yml
    with:
      device: z3q
      device_name: Galaxy S20 Ultra 5G
      
